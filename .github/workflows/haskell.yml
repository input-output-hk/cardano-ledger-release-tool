name: Haskell CI

on:
  push:
    branches: ["master", "main"]
    tags:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        ghc: ['9.6', '9.8', '9.10', '9.12']
      fail-fast: false

    name: Build with GHC ${{ matrix.ghc }} on ${{ matrix.os }}

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Cache
      uses: actions/cache@v5
      with:
        path: ~/.cabal
        key: ${{ runner.os }}-${{ matrix.ghc }}-${{ hashFiles('**/*.cabal') }}-${{ hashFiles('**/cabal.project') }}
        restore-keys: ${{ runner.os }}-${{ matrix.ghc }}-

    - name: Haskell setup
      uses: haskell-actions/setup@main
      with:
        ghc-version: ${{ matrix.ghc }}

    - name: Configure
      run: |
        cabal update
        cabal configure --enable-tests --enable-benchmarks

    - name: Build dependencies
      run: cabal build all --only-dependencies

    - name: Build
      run: cabal build all

    - name: Install nix
      uses: cachix/install-nix-action@v31

    - name: Run tests
      run: cabal test all

    - name: Lint changelogs
      if: matrix.ghc == '9.10.3'
      run: |
        cabal run -- cleret changelogs format -i $(git ls-files '*CHANGELOG.md')
        git diff --exit-code

    - name: Check nix hashes
      if: matrix.ghc == '9.10.3'
      run: cabal run -- cleret nix hashes -v

  build-complete:
    name: 'Haskell CI: build'
    if: always()
    needs: build
    runs-on: ubuntu-latest
    steps:
    - run: |
        case '${{ needs.build.result }}' in
          success|skipped) exit 0 ;;
          *)               exit 1 ;;
        esac
